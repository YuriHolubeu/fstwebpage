/** Email notifications — reserved for future Django integration. */
export async function notifySignupRecord () {
  return undefined
}

export async function notifySignupRecords () {
  return undefined
}
